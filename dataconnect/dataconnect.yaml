specVersion: "v1"
serviceId: "vaadvocate"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vaadvocate-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
